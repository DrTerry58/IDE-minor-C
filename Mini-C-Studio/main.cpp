// ============================================================================
// 文件名：main.cpp
// 职责：程序入口
// 负责人：组长 A
//
// C 同学的改动：仅新增 2 行（第 12 行 include、第 16 行调用欢迎界面）。
//               其余一行未动。
// ============================================================================

#include "MainWindow.h"
#include "SplashScreen.h"     // <- C 同学新增：启动欢迎界面

int main()
{
    // 1. 先显示启动欢迎界面（LOGO + 进度条，2 秒；点击或按键可跳过）  <- C 同学新增
    showSplashScreen(MINIC_SPLASH_MS);

    // 2. 创建主窗口对象（A 原有）
    MainWindow app;

    // 3. 初始化图形窗口（A 原有）
    app.initWindow();

    // 4. 进入主消息循环（A 原有）
    app.mainLoop();

    // 5. 关闭图形窗口（A 原有）
    closegraph();
    return 0;
}
