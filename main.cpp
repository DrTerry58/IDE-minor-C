// ============================================================
// 文件名：main.cpp
// 职责：程序入口，创建主窗口并启动消息循环
// 负责人：组长 A
// ============================================================

#include "MainWindow.h"

int main()
{
    // 1. 创建主窗口对象
    MainWindow app;

    // 2. 初始化图形窗口（800x600）
    app.initWindow();

    // 3. 进入主消息循环（死循环，直到用户关闭窗口）
    app.mainLoop();

    // 4. 关闭图形窗口（实际上 mainLoop 是死循环，这行通常执行不到）
    closegraph();
    return 0;
}
